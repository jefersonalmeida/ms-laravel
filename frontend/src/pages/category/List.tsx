import * as React from 'react';
import {Page} from '../../components/Page';

interface CategoryProps {

}

const List = (props: CategoryProps) => {
  return (
      <Page title="Listagem de Categorias"/>
  );
};

export default List;
